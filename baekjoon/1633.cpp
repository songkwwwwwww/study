

const int MAX_N = 8;
const int MAX_M = 8;

inline bool is_range(int x, int y){
    return (0 <= x && x < N && 0 <= y && y < N);
}

int main(){
    freopen("1633.txt", "r", stdin);
    //setbuf(stdout, NULL);
    int 
    while(scanf("%d %d"))
}